<% @title = 'Admin' %>

<%= link_to 'Home', root_path %>/
<%= link_to 'Admin', admins_path %>/
<%= link_to 'Pages', pages_path %>


<h1>Contenus éditable:</h1>

<ul>
<% @pages.each do |page| %>
	<li><%= link_to page.name.capitalize, edit_page_path(page) %></li>
<% end %>
<ul>
