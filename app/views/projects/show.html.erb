<% provide(:title, @project.name) %>
<% provide(:h1_title, @project.name) %>

<h2 style="text-align:center" id="project_name"><%= raw @project.name %></h2>

<article>
	<p>
		<div id="project_content" data-mercury="full">
			<%= raw @project.content %>
		</div>
	</p>
</article>