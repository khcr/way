<%= link_to 'Home', root_path %>/
<%= link_to 'Admin', admin_path %>/
<%= link_to 'News', admin_news_path %>/
<%= link_to 'Editer', edit_admin_new_path(@new) %>

<h1>Editer une new</h1>

<%= form_for [:admin, @new] do |f| %>

	<%= render 'fields', f: f %>

	<%= f.submit "Editer" %>

<% end %>