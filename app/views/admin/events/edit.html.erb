<% @title = 'Admin' %>

<%= link_to 'Home', root_path %>/
<%= link_to 'Admin', admin_path %>/
<%= link_to 'Programme', admin_events_path %>/
<%= link_to 'Editer', edit_admin_event_path(@event) %>

<%= render '/shared/flash', flash: flash %>

<h1>Editer un événement</h1>

<%= form_for [:admin, @event] do |f| %>

	<%= render 'fields', f: f %>

	<% if @event.image_id? %>
		<% image = Image.find(@event.image_id) %>
		<%= image_tag image.image.url %>
		<%= f.label :image_id, "Supprimer" %>
		<%= f.radio_button :image_id, "" %>
	<% end %>

	<%= render 'images', f: f %>

	<%= f.submit "Editer" %>
	
<% end %>

<%= render 'shared/upload' %>